{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "splash",
  "description": "Capabilities for the TurboTalk splash window",
  "windows": ["splash"],
  "permissions": [
    "core:window:allow-close",
    "core:app:allow-version"
  ]
}
