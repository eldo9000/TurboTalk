# Rust
target/
Cargo.lock.bak
**/*.rs.bk

# Node
node_modules/
dist/
.vite/
*.log
npm-debug.log*

# Tauri
src-tauri/target/
src-tauri/gen/schemas/
src-tauri/WixTools/

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Local config / secrets
.env
.env.*
!.env.example

# Whisper models (downloaded at runtime, never commit)
models/
*.ggml
*.bin

# Build artifacts
*.dmg
*.app
*.pkg
build/artifacts/

# Fetched Windows sidecars (run `npm run fetch-sidecars` on a Windows host
# to repopulate). The macOS arm64 binary + dylibs stay committed.
src-tauri/binaries/whisper-cli-x86_64-pc-windows-msvc.exe
src-tauri/binaries/whisper.dll
src-tauri/binaries/ggml.dll
src-tauri/binaries/ggml-base.dll
src-tauri/binaries/ggml-cpu.dll
.claude/
