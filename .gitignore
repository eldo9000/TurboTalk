# Build artifacts
target/
dist/
node_modules/
*.dmg
*.app
*.pkg
build/TurboTalk-*
.build-dmg/

# Pre-built binaries — download with `npm run fetch-binaries`
# (or copy from a previous build). Not tracked in git.
src-tauri/binaries/
src-tauri/resources/silero_vad.onnx
.claude/

# macOS
.DS_Store
