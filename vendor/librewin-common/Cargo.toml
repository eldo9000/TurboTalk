[package]
name = "librewin-common"
version = "0.1.0"
description = "Shared utilities for LibreWin Freedom Apps (vendored from eldo9000/Libre-Apps@v0.1.3)"
authors = ["LibreWin"]
edition = "2021"
rust-version = "1.77"

[dependencies]
tauri      = { version = "2", features = [] }
serde      = { version = "1", features = ["derive"] }
serde_json = "1"
chrono     = { version = "0.4", features = ["serde"] }
once_cell  = "1"
xattr      = { version = "1", optional = true }

[features]
default = []
# Enable xattr-based file tagging (get_tags / set_tags)
tags = ["dep:xattr"]
